# Базовый образ с Python 3.11
FROM python:3.11

# Создаем директорию для вашего приложения внутри контейнера
WORKDIR /app

# Копируем файлы вашего проекта в контейнер
COPY . /app

# Устанавливаем все зависимости (если используете requirements.txt)
# Если зависимостей нет, пропустите этот шаг
RUN pip install -r requirements.txt

# Запуск main.py как основного файла приложения
CMD ["python", "main.py"]
